module.exports.function = function connectWinnerUrl (url) {
  const console = require('console');

  let winnerURL = 'link';

  winnerURL = 'http://info.nec.go.kr/main/showDocument.xhtml?electionId=0020200415&topMenuId=EP&secondMenuId=EPEI01';

  result = winnerURL;

  return result;
}
